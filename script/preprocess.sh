# Preprocess script for preparing datasets (DACO, InsightBench or ALL).
# Usage:
#   python preprocess_work_dir.py # Process both datasets by default (DACO and InsightBench)
#   python preprocess_work_dir.py --dataset DACO # Process only specified dataset
#   python preprocess_work_dir.py --target_root /path/to/target  # Specify a custom target root directory

python preprocess_work_dir.py